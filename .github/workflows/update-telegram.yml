name: Update Telegram Data
on:
  schedule:
    - cron: '0 */6 * * *'  # Каждые 6 часов
  workflow_dispatch:  # Ручной запуск

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          
      - name: Reset to latest remote state
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Actions Bot"
          git fetch origin main
          git reset --hard origin/main
          
      - name: Get Telegram subscriber count
        run: |
          # Получаем данные из Telegram
          curl -s -A "Mozilla/5.0" "https://t.me/s/analinhistory" -o telegram.html
          
          # Извлекаем количество подписчиков
          SUBSCRIBERS=$(grep -oP '\d+(?:,\d+)*(?=\s*subscribers?)' telegram.html | head -1)
          
          # Fallback если данные не получены
          if [ -z "$SUBSCRIBERS" ]; then
            echo "⚠️ Не удалось получить данные, используем приблизительное значение"
            SUBSCRIBERS="950+"
          fi
          
          echo "📊 Найдено подписчиков: $SUBSCRIBERS"
          
          # Создаем обновленный JSON файл
          mkdir -p docs
          cat > docs/telegram-data.json << EOF
{
  "subscribers": "$SUBSCRIBERS",
  "updated": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "channel": "@analinhistory"
}
EOF
          
      - name: Update and push changes
        run: |
          # Проверяем есть ли изменения
          if git diff --exit-code docs/telegram-data.json > /dev/null 2>&1; then
            echo "📊 Данные не изменились, обновление не требуется"
            exit 0
          fi
          
          # Читаем текущее значение подписчиков
          CURRENT_SUBS=$(cat docs/telegram-data.json | grep -o '"subscribers": "[^"]*"' | sed 's/"subscribers": "\(.*\)"/\1/')
          
          echo "🔄 Обновляем данные: $CURRENT_SUBS подписчиков"
          
          # Коммитим изменения
          git add docs/telegram-data.json
          git commit -m "📊 Подписчики: $CURRENT_SUBS ($(date '+%d.%m %H:%M'))"
          
          # Отправляем с автоматическим resolve конфликтов
          git push origin main || {
            echo "⚠️ Конфликт обнаружен, применяем force push..."
            git push origin main --force-with-lease
          }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
