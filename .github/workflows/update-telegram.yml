name: Update Telegram Data
on:
  schedule:
    - cron: '0 */6 * * *'  # Каждые 6 часов
  workflow_dispatch:  # Ручной запуск

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          
      - name: Configure Git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          
      - name: Sync with remote
        run: |
          git fetch origin main
          git reset --hard origin/main
          
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl jq
          
      - name: Fetch Telegram data
        run: |
          curl -s "https://t.me/s/analinhistory" -o telegram.html
          
          SUBSCRIBERS=$(grep -oP '\d+(?:,\d+)*(?=\s*subscribers?)' telegram.html | head -1)
          
          if [ -z "$SUBSCRIBERS" ]; then
            SUBSCRIBERS="900+"
          fi
          
          echo "{
            \"subscribers\": \"$SUBSCRIBERS\",
            \"updated\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\",
            \"channel\": \"@analinhistory\"
          }" > docs/telegram-data.json
          
      - name: Commit and push with retry
        run: |
          if git diff --quiet docs/telegram-data.json; then
            echo "Нет изменений в данных"
            exit 0
          fi
          
          git add docs/telegram-data.json
          git commit -m "🔄 Обновление данных Telegram: $(date '+%d.%m.%Y %H:%M')"
          
          # Попытка push с автоматическим retry
          for i in {1..3}; do
            if git push origin main; then
              echo "✅ Успешно отправлено с попытки $i"
              break
            else
              echo "⚠️ Попытка $i неудачна, синхронизируем и пробуем снова..."
              git fetch origin main
              git rebase origin/main
            fi
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
